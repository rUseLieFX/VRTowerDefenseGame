using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Nem tartom fontosnak még ennek az elkészítését, mert pár pillanat alatt megvan, és a tesztelésben semmit nem segít.
public class Desktop_Controller : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
